# Velox V10 Prometheus Configuration
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "velox"
    static_configs:
      - targets: ["velox:8080"]
    metrics_path: "/metrics"
    scrape_interval: 10s
